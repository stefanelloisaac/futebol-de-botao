import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stefanelloisaac.futebolbotao',
  appName: 'FutebolBotao',
  webDir: 'build'
};

export default config;
